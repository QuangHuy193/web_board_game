import Button from "../ui/Button";

const Header = () => {
  return (
    <div>
      <Button variant="primary" size="sm" scale={105}>
        Đăng nhập
      </Button>
    </div>
  );
};

export default Header;
